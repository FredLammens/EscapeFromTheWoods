CREATE TABLE [dbo].[WoodRecords] (
    [recordId] INT NOT NULL,
    [woodID]   INT NOT NULL,
    [treeID]   INT NOT NULL,
    [x]        INT NOT NULL,
    [y]        INT NOT NULL,
    PRIMARY KEY CLUSTERED ([recordId] ASC)
);

